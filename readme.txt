Never use “SetTimeOut” and “SetInterval” as alternatives to “Eval”
[] is better than “new Array();”
{} is better than “new Object();”
Avoid Eval
Avoid With
Avoid new Function(code)
Avoid Global Variables

use === instead ==
use single return




Warn ForIn
Warn try-catch in loop
Warn multiple var statements


performance tips
childNodes[0] == > .firstChild
for(i=0;i<v.length;i++ )  ==> for(i=0 ,l = v.length;i<l;i++)


minification tips
100000 => 1e5


